unit frm_replacecondition;

{$mode objfpc}{$H+}

interface

uses
  Classes, SysUtils, FileUtil, Forms, Controls, Graphics, Dialogs, StdCtrls;

type

  { TForm1 }

  TForm1 = class(TForm)
    btnAdd: TButton;
    btnAnd: TButton;
    btnLeftBracket: TButton;
    btnNOT: TButton;
    btnOR: TButton;
    btnRightBracket: TButton;
    Button1: TButton;
    Button2: TButton;
    combVar: TComboBox;
    combOperator: TComboBox;
    Edit1: TEdit;
    Label1: TLabel;
    Label2: TLabel;
    Label3: TLabel;
    Label4: TLabel;
    Label5: TLabel;
    Label6: TLabel;
    memExpress: TMemo;
    txtValue: TEdit;
    VarList: TListBox;
    procedure FormCreate(Sender: TObject);
  private

  public

  end;

var
  Form1: TForm1;

implementation

{$R *.lfm}

{ TForm1 }

procedure TForm1.FormCreate(Sender: TObject);
begin

end;

end.

